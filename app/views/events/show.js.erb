$modal = $('.modal'),
        $modalBody = $('.modal .modal-body'),
        $modalHeading = $('.modal .modal-heading');
$modalHeading.html("<%= @event.name %>");
$modalBody.html("<%= escape_javascript(render @event) %>");
$modal.modal();